#!/bin/sh
# Пустая заглушка вместо системного `open`.
#
# Сервер v-down при старте зовёт `open <адрес>` и открывает вкладку браузера.
# В нативном приложении страница уже показана в своём окне, вторая копия в
# Safari только мешает. Оболочка кладёт эту папку первой в PATH дочернего
# процесса, поэтому `open` внутри сервера превращается в no-op.
#
# Когда в сервере появится флаг «-no-open», заглушку можно удалить вместе
# со строкой childEnvironment() в app/VideoDownloaderApp.swift.
exit 0
