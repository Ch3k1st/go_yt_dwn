// Конфигурация связи с программой Video Downloader.
//
// ВАЖНО: этот файл переписывается программой при распаковке расширения
// (кнопка «Расширение» → POST /api/extension/install). Плейсдержатели ниже
// заменяются на реальные значения, поэтому руками сюда ничего вписывать не надо.
// Если вы видите __VDOWN_TOKEN__ — папка скопирована мимо программы,
// распакуйте расширение заново из самой программы.
self.VDOWN_CONFIG = {
  // Общий секрет с программой. Без него сервер отвечает 403 —
  // чтобы произвольный сайт не мог командовать локальной программой.
  token: "__VDOWN_TOKEN__",
  // Порт, на котором программа слушала в момент распаковки.
  preferredPort: "__VDOWN_PORT__",
  // Диапазон, который перебираем, если preferredPort молчит
  // (программа берёт свободный порт, когда 8080 занят).
  ports: [8080, 8081, 8082, 8083, 8084, 8085, 8086, 8087, 8088, 8089, 8090],
  // Версия протокола обмена. Расхождение с ответом /api/ping — предупреждение в popup.
  protocol: 1,
  // Версия программы на момент распаковки (для диагностики).
  appVersion: "__VDOWN_APP_VERSION__"
};
